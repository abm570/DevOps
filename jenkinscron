pipeline {
    agent any

    triggers {
        cron('* * * * *')
    }

    stages {
        stage('Test Cron') {
            steps {
                echo "Triggered by cron"
            }
        }
    }
}
